# Agent 服務依賴 — 版本固定（>= 下限, < 上限），避免 breaking changes

# Web 框架
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# HTTP 客戶端
httpx>=0.27.0,<1.0.0

# 配置管理
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# 定時任務
apscheduler>=3.10.0,<4.0.0

# LLM 供應商 SDK
qoder-agent-sdk>=0.1.0

# RAG: 向量數據庫 + 中文 embedding
pymilvus>=2.4.2,<3.0.0
sentence-transformers>=2.7.0,<7.0.0

# Python 版本約束: >=3.10,<3.15
