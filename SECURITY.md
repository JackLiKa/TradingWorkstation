# 依赖安全政策 (Security Policy)

## 报告漏洞

如果你发现安全漏洞，请**不要**通过公开 Issue 报告。

请发送邮件到项目维护者邮箱，描述：

- 漏洞类型和影响范围
- 复现步骤
- 受影响的版本
- 建议的修复方案（如有）

我们会在收到报告后 48 小时内确认，并在修复后公开致谢。

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| 最新 main 分支 | ✅ 支持 |
| 旧版本 | ❌ 不支持 |

## 安全最佳实践

- **不要提交 `.env` 文件**：包含数据库密码、API Key 等敏感信息
- **API Key 管理**：LLM API Key 只放在 `agent/.env`，不入库
- **数据库密码**：只在 `.env` 中配置，`.env.example` 中留空
- **CORS 配置**：生产环境应将 `CORS_ALLOWED_ORIGINS` 限制为实际域名
- **HTTPS**：生产环境必须通过反向代理（Nginx/Caddy）启用 HTTPS
